# NEXXOS MVP - Project Instructions

## Critical Architecture Decisions

### S3 Upload Strategy (IMPORTANT - June 2026)
- **Abacus hosted S3** uses temporary STS credentials (`ASIA*` prefix) that rotate frequently
- **Presigned URLs DO NOT WORK** from the mobile app because credentials expire between URL generation and mobile upload
- **Solution**: Use **proxy upload** (`POST /api/upload/direct`) — mobile sends file to backend via multipart/form-data, backend uploads to S3 with fresh credentials
- The presigned URL flow still exists for backwards compatibility but should NOT be used for mobile uploads on the Abacus dev backend
- **Direct S3 URLs** (e.g., `https://bucket.s3.region.amazonaws.com/key`) work fine for public files (reading/viewing)

### S3 Credential Resolution (CRITICAL)
- `aws-config.ts` uses a 3-tier credential resolution:
  1. **Explicit IAM keys** from DB `app_config` table (keys: `API_AWS_ACCESS_KEY_ID`, `API_AWS_SECRET_ACCESS_KEY`) — permanent, cached
  2. **Local credential file** at `/aws_credentials/.aws/hosted_storage_credential_json` — checked for expiration
  3. **S3 refresh location** (`ABACUS_AWS_REFRESH_LOCATION` env var) — uses `ABACUS_AWS_*` bootstrap credentials to fetch fresh `HostedStorageCredentials` from S3
- The production container may have stale local credentials after suspension. The refresh mechanism (tier 3) solves this by fetching fresh STS tokens from the refresh bucket.
- **NEVER use `new S3Client({})`** (bare) — the default chain picks up `ABACUS_AWS_*` env vars which do NOT have access to the app bucket.
- **NEVER use `fromProcess()`** — it reads the local file which may be stale in prod.

### URL Resolution in Chat Messages
- Some old messages in DB have bare S3 keys (e.g., `37513/public/uploads/...`) instead of full URLs
- `resolveS3Url()` in `chat.service.ts` handles this by prepending the full S3 domain when URLs don't start with `http`

### Backend Environments
- **Dev (Abacus)**: `https://nexxos-api.abacusai.app` — uses hosted_storage STS credentials (auto-refreshed)
- **QA (Render)**: `https://api.nexxos.app/` — uses long-lived IAM credentials from env vars
- When switching between backends, the upload strategy matters: presigned URLs work on QA but NOT on Abacus dev

## Tech Stack
- Expo SDK 54, React Native, NestJS
- Android is the priority platform (Venezuelan market)
- User language: Spanish

## Git/QA
- QA backend repo: `NexxosDev/nexxos-backend` (branch `master`)
- No frontend repo exists
- Never push to QA repo without explicit user permission
