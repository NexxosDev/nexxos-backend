// Fuente única de la versión visible en la app (pantalla "Sobre NEXXOS").
// IMPORTANTE: incrementar BUILD_NUMBER en cada build, junto con
// android.versionCode e ios.buildNumber en app.json (deben coincidir).
export const APP_VERSION = '1.0.0';
export const BUILD_NUMBER = 19;
